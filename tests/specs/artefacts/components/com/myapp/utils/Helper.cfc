/**
 * Helper Component
 * This component does NOT have a corresponding test file
 * This is a placeholder file used by UnitTestChecker tests
 */
component {

    function init() {
        return this;
    }

    function formatString(required string str) {
        return ucase(str);
    }

}
